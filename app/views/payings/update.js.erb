<% if @paying && @paying.errors.any? %>
  $('.show-notice').html('<% @paying.errors.full_messages.each do |msg| %>\
    - <%= msg %><br>\
  <% end %>');
<% else %>
  window.location.href = '<%= orders_path %>';
<% end %>
