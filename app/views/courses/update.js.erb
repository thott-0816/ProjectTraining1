<% if @course.errors.any? %>
  $('.error-message').addClass('alert alert-danger');
  $('.error-message').html('<% @course.errors.full_messages.each do |msg| %>\
    - <%= msg %><br>\
  <% end %>');
<% else %>
  window.location.href = '<%= courses_path %>';
<% end %>
